# Метрики с разбивкой по очередям: без этого плагин отдаёт только агрегаты,
# и глубину конкретной очереди (avatars.process, лестница retry, DLQ)
# не увидеть. Допустимо на десятках очередей; на тысячах раздуло бы scrape.
prometheus.return_per_object_metrics = true
