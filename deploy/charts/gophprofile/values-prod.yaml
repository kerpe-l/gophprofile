# Prod: инфраструктура вне кластера. Обязательно задать secrets.existingSecret
# и config.s3Endpoint — без них релиз не устанавливается.

config:
  appEnv: prod
  s3Endpoint: ""
  s3UseSSL: true

secrets:
  create: false
  existingSecret: ""
